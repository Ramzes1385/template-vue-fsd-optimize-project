export {
  DEFAULT_LOCALE,
  FALLBACK_LOCALE,
  isAppLocale,
  LOCALE_STORAGE_KEY,
  SUPPORTED_LOCALES,
} from './config'

export { messages } from './messages'

export type { AppLocale, LocaleConfig, LocaleDirection } from './types'
